{% extends 'base.html.twig' %}

{% block title %}Home {{ controller_name }}!{% endblock %}
{% block stylesheets %}
    <style>

    </style>
{% endblock %}
{% block body %}

    <div class="row">
        {% for game in player.games %}
            <div class="col-3">
                <div class="card">
                    <div class="front">
                        {{ game.idOWL }}
                        {{ game.matches.teamA }}
                    </div>
                    <div class="back">
                        {{ game.name }}
                    </div>

                </div>


            </div>
        {% endfor %}
    </div>

{% endblock %}
